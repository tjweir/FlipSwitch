Install Java, Simple Build Tool, and Git

Clone this repository with:

git clone git://github.com/tjweir/FlipSwitch.git

Then cd into FlipSwitch and type:

"sbt"


At the new sbt prompt, type:
"update"
<wait for stuff to happen>
then:
"jetty-run"

When the server is up and running, point your browser to:
http://localhost:8080/

Voila, a running Lift app

@dpp is a rockstar.